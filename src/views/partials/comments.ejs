<% if (!comments || comments.length === 0) { %>
  <p class="text-gray-500">No comments yet.</p>
<% } %>

<% comments.forEach(function(comment) { %>
  <%- include("comment", { comment, user, timeAgo }) %>
<% }) %>